# alandastudillo.github.io

Personal page for Dr Aland Astudillo, served from `docs/` by GitHub Pages.

Plain HTML, no build step, no dependencies. `.nojekyll` is present so GitHub serves the file
directly instead of running it through Jekyll — the previous Jekyll scaffold was never filled
in and rendered an empty page.

Placeholder until `alandastudillo.com` is live, at which point this becomes a redirect.
